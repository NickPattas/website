A vertex shader processes **each vertex of a 3D model**. It **transforms 3D coordinates into 2D screen space** and passes data like **color, texture coordinates, normals etc**. down to [[Graphics Pipeline]].

**Runs once per vertex.**
